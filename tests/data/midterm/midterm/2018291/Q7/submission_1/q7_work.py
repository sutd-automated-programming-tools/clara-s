# MID-TERM EXAM: QUESTION 7
count = []
def decompose(pence):
    for i in range(pence):
        n-1
    if n == 0:
        count.append(1)
    #count the number of methods by appending to count once n ==0
    #then count length of list count to get the number of methods