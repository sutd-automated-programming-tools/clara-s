def determinant(matrix):
    for ls in matrix:
        for subls in ls:
            if len(subls) != len(ls) :
                return None
            else:
                pass
        if len(ls) == 1:
            det(M) = matrix(ls)
        elif len(ls) == 2:
            det(M) = 
                    