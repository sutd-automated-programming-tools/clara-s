# MID-TERM EXAM: QUESTION 5
import math
def nroot(n,t,num):
    x = 1
    x2 = num
    f(x) = x2
    fp(x) = 
    for x in range(t,-1,-1): #Iterations start at t, stop at 0, where is reduced each time
        xt += x - (f(x) / fp(x))


def nroot_complex(n,t,num):
    pass