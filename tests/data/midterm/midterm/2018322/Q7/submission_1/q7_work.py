import numpy as nump
coinslist = [1, 2, 5, 10, 20, 50, 100, 200]
def decompose(pence):
    
    while i < range (len(coinslist)):
        combi = 
        pence =
        i+=1
        
    return combi
