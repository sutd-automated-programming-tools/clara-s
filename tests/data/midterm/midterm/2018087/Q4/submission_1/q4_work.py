def determinant(matrix):
    M = {}
    if n = 1:
        M = [a]
        det(M) = a
        return det(M)
    elif n = 2:
        M = {1:[a,b], 2:[c,d]} #creating a 2x2 matrix
        det(M) = a*d - b*c
        return det(M)
    elif n = 3:
        M = {1:[a,b,c], 2:[d,e,f], 3:[g,h,i]} #creating a 3x3 matrix
        det(M) = a* det() #input the formula below (3)
        return det(M)
        
    else:
        return None #returning none if input not in matrix dimension 