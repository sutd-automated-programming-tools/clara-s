def determinant(matrix):
	for i in range(len(matrix)): #calls the list within the overall list
        n = len(matrix)
        if n != len(i) and (n >3 or n<1):
            return False
        else:
            if n == 1:
                det1 = n
                return det1
            elif n ==2:
                for j in i[0]:
                    j[1] + 
                    
                    
           