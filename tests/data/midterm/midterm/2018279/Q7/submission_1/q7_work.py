# MID-TERM EXAM: QUESTION 7

def_decompose(pence):
    a=1
    b=2
    c=5
    d=10
    e=20
    f=50
    g=100
    h=200
    range(pence, -1, a)
    range(pence, -1, b)
    range(pence, -1, c)
    range(pence, -1, d)
    range(pence, -1, e)
    range(pence, -1, f)
    range(pence, -1, g)
    range(pence, -1, h)