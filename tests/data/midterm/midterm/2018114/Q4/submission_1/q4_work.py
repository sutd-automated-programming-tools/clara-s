def determinant(matrix):
    for len(matrix)==1:
        det(matrix) = matrix[0]
        return det(matrix)
    for len(matrix)==2:
        alist=matrix[0]
        blist=matrix[1]
        det(matrix) = (alist[0]*blist[1])-(alist[1]*blist[0
        return det(matrix)
    for len(matrix)==3:
       
                                      