def is_matrix(ls):
    length = len(ls)
    if length<1 or lenght>3:
        return False
    elif True:
        counter = 0
        for i in ls:
            lengths = len(i)
            if length == len:
                counter+=1
    elif counter == n:
        return True
    else:
        return False
def det2(matrix):
    return det = matrix[0][0]*matrix[1][1]- matrix[0][1]*matrix[1][0]
def determinant(matrix):
    if is_matrix(matrix):
        if len(matrix)==1
            det = matrix[0]
        if len(matrix) ==2:
            det = det2(matrix)
        if len(matrix) ==3:
            det = 
    else:
        return None